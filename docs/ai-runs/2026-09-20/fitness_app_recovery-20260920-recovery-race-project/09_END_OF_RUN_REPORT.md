# Fit Log — דוח גרסת עבודה · 2026-09-20

מצב: YELLOW — פיתוח נבדק מקומית; לא פורסם ולא מוזג ל־main.

## מה השתנה
- `/running/project`: יעד 5–10 חצאי מרתון ב־2026, המרוצים שדווחו, מועדים משוערים מסומנים. שבוע ראשון–שבת, העדפות הליכון, נפח/איכות/התאוששות. ההמלצה המקורית נשמרת לצד הבחירה והיסטוריית גרסאות.
- מנוע מקומי מבוסס כללים; מופעל בכניסה לשבוע. זהו מימוש בסיסי, לא סוכן LLM ולא תזמון ברקע. אין תוספת עלות או חיבור AI חיצוני.
- קישור ביצוע לתוכנית ולמרוץ בתוך רשומת הריצה הקיימת. נתוני היומן משמשים לשבוע הבא כאשר יש היסטוריה מספקת.
- שדות נפרדים לדקות ולשניות, מהירות/מרחק עשרוניים, שמירת ההקלדה האחרונה לפני סיום/ניווט. חישוב מחדש של קצב נגזר בלי לדרוס ערך ידני.
- היסטוריה משותפת לריצה, בית ומכון וקיצורי דרך עם שמות ברורים.
- סטים שונים בהזנה מרוכזת, וריאציות שכיבות סמיכה ובטן, חבל ומשקולות; שימור סוג מדידה משוקלל בתרגיל מותאם. שותף לאימון נשמר כהקשר, ללא פרופיל ילד.

## ראיות ומגבלות
- 363 בדיקות ספרייה עברו. 61 בדיקות המסכים הקיימות עברו; 6 בדיקות UI חדשות עברו (כולל השדה, דיווח מקושר, מספרים עשרוניים, גיבוי, סטים ויציאה מיידית להיסטוריה/מסך הפרויקט).
- TypeScript, build ו־lint לקבצים ששונו עברו. lint כלל המאגר מכיל ליקויים קודמים; לא בוצע עיצוב גורף לקבצים שאינם קשורים.
- תוקן test harness שיצר `<html>` בתוך `<div>` ותקע אינטראקציות; הוא מרנדר כעת ל־Document. אימות אינטראקציות הוא jsdom, לא בדיקת דפדפן חזותית.
- הדפדפן לא זמין בשלב הסיום. בדיקת RTL 390×844 הנדרשת ב־AGENTS.md לא הושלמה, ולכן אין פרסום או מיזוג.
- ההתקנה לצורך הבדיקות נעשתה ב־npm ללא שינוי lockfile; Bun אינו זמין בסביבה. יש לאמת גם את סביבת ה־lockfile הקנונית לפני פרסום.
- אין שינוי Auth, RLS, מיגרציות או Supabase. הפרויקט המאומת נשאר `fusrapommtdqwfglkmks`. אימות הענן עם משתמש מחובר נשאר פתוח; אין הבטחה לסנכרון יומני האימונים בין מכשירים.
- הנתונים החדשים מקומיים ונכללים בגיבוי הקיים; גיבוי־שחזור נבדק. מועדי המרוצים לא הומצאו.
- Control Tower / Chief of Staff: pending, מסלולי קליטה לא אומתו. אירוע outbox נשמר; אין טענה למסירה.

## הצעד הבא
בדיקה חזותית ב־390×844 ב־RTL, ובדיקת התנהגות באפליקציה בפועל. לאחריה תיקונים אם נדרשים, מיזוג ופרסום לכתובת הקיימת. בנפרד: אימות Phase 1 עם משתמש מחובר והשלמת מנוע סוכן ותזמון לאחר בחירת חיבור זמין.

לא נדרשת כרגע החלטה מאריאל. נדרשת יכולת בדיקת דפדפן להשלמת שער הפרסום.

חותמת הדוח: 2026-09-20T20:16:16+03:00

---

# המשך ריצה · Claude Code · 2026-09-20 (ערב)

מצב: **GREEN לקוד ולבדיקות** — סקירה מלאה של PR #1, QA דפדפן אמיתי ב-390×844 RTL, תיקון כל הליקויים שנמצאו, וכל שערי האימות עברו. מיזוג ופרסום מתועדים ב-`10_HANDOFF_AND_NEXT.md` ובלדג'רים לאחר ביצועם.

## סביבה
- Bun 1.4.2 (ה-lockfile הקנוני) · Node 24.19.0 · Windows 11. `bun install --frozen-lockfile` — 523 חבילות, ללא שינוי lockfile.
- QA דפדפן: Chrome אמיתי (channel `chrome`) דרך `playwright-core` מתיקיית scratchpad מחוץ לריפו; viewport 390×844, DPR 2, `isMobile`, `hasTouch`, locale `he-IL`, timezone `Asia/Jerusalem`. **לא נוספה תלות לריפו.** הרחבת Claude-in-Chrome לא הייתה מחוברת ולכן לא שימשה.
- כל צילום מסך לווה בבדיקת DOM: `scrollWidth ≤ clientWidth`, אלמנטים החורגים מרוחב המסך, ומטרות מגע קטנות מ-40px. צילומים: `assets/mobile-qa-390x844/`, יומן מלא: `assets/mobile-qa-390x844/final-qa-log.txt`.

## ליקויים שנמצאו ב-QA ותוקנו
| # | ליקוי | תיקון |
|---|---|---|
| 1 | **היסטוריית גרסאות התנפחה בכל הקלדה** — `updateWeek` צילם revision בכל שינוי שדה/אישור/דחייה (6 גרסאות אחרי שתי פעולות אמיתיות). | `updateWeek` שומר revision רק ב-`snapshotPrevious` — כלומר בעדכון מפורש (`regenerateWeek`). עריכות/אישור/דחייה מעדכנים במקום. בדיקה: `model.test.ts`. |
| 2 | **רענון של `/running/{id}` ו-`/running/{id}/edit` החזיר HTTP 404** — ה-loader זרק `notFound()` בשרת, שאין לו localStorage. כתובת הטיוטה שהטופס מציב (`replaceState`) לא שרדה רענון. **קיים גם ב-live היום** (אומת ב-`curl`: 404). | ה-loader לא מכריע בשרת (`typeof window === "undefined"` → מחזיר `{id}`); הרכיב מיישם את דפוס ADR-0039 (`useHydrated` ואז `notFound()`). רענון → 200 והטופס/הפרטים נטענים. נרשם **R-43** למסלולים דומים שלא בהיקף. |
| 3 | **קצב נגזר הוצג כערך שהוקלד** בעריכת ריצה קיימת — השדה התמלא בערך המחושב, ה-footnote נעלם ולא התעדכן חי. | `fromExisting` מנקה ערכי `derived` (קצב/מהירות) מהטופס; הם מחושבים חי מזמן+מרחק ונשמרים מחדש כ-`derived` בכל שמירה. ערך ידני לעולם לא נדרס (אומת: קצב ידני 5:30 נשמר אחרי שינוי משך). |
| 4 | **תאריכים בפורמט MM-DD** (`09-22`) וטווח ISO — דו-משמעי לקורא עברית; טווחי מספרים התהפכו ב-RTL (`10–5`). | `formatDayMonth` → `22.9`, `formatWeekRange` → `20.9 – 26.9.2026`, `dir="ltr"` על טווחים. |
| 5 | **מטרות מגע 36px** בכל המסך החדש (Input/Button ברירת מחדל 36px, `summary` 24px) לצד select של 44px. | `min-h-11` (44px) לכל כפתור, שדה, קישור ו-summary במסך הפרויקט, בהיסטוריה ובקיצורי הדרך. |
| 6 | **קיצורי הדרך הופיעו בכל מסך**, כולל בתוך טופס הריצה ובמסך הפרויקט עצמו (קישור לעצמו). | מוצגים רק במסכים ללא קישור חזרה (מסך ראשי, `עוד`); מסכי פרטים/טופס שומרים "חזרה" + ניווט תחתון. |
| 7 | **placeholder של קצב הציג "37 / 20"** (העתק של דוגמת המשך). שדות משך/קצב נדחסו לחצי שורה. | `DurationField` מקבל `placeholder`; קצב מציג את הערך הנגזר כרמז (`5 / 58`). משך וקצב תופסים שורה מלאה; מרחק ליד התאריך. |
| 8 | **"דיווח מהיר" הציג 40 תרגילים אלפביתיים כולל תרגילי מכון** (לחיצת חזה במוט, דדליפט); שכיבות סמיכה, כפיפות בטן וחבל נחתכו. | המסך מציג את הקטלוג הביתי המצומצם לפי קבוצות (ADR-0029), "התרגילים שלי" (מותאמים, אחרי hydration — R-27), ו"תרגיל מותאם" inline (משקולות → `weight_reps`). |
| 9 | **הוספת סטים בבת אחת השאירה "סט 1" ריק** (0 חזרות, לא הושלם) — הסיכום הראה 3/4 סטים, יציבות 64%. | סטים ריקים שטרם נגעו בהם מתמלאים קודם; רק אחר כך נוספים חדשים. בדיקה ב-`fitnessRecovery.test.tsx`. |
| 10 | שלושה נתיבי שמירה (autosave / יציאה / שמור-וסיים) כתבו רשומות שונות (`""` מול `null`). | `toRunPatch()` יחיד לכל הנתיבים. |
| 11 | פרטי ריצה לא הראו את הקשר לתוכנית. | שורת "בתוכנית: שני 21.9 · ריצה קלה · לפרויקט ←" עם קישור. |
| 12 | `model.ts` — שגיאת prettier אחת (הקובץ היחיד מבין 26 הקבצים ששונו שלא עבר lint). | תוקן. |

## אימות
- `bun install --frozen-lockfile` ✅ · `bun run typecheck` ✅ · `bun run build` ✅.
- יחידה: **364 בדיקות ב-25 קבצים** (`bun run test:unit`). מסכים/router בבידוד (שיטת הריפו, קובץ-קובץ): **71 בדיקות ב-12 קבצים** — `fitnessRecovery` 9 · `durationField` 1 · השאר ללא שינוי. **סה״כ 435.** שני הקבצים החדשים נרשמו ב-`test:router`.
- lint: הריפו כולו **390 בעיות (382 שגיאות, 8 אזהרות)** — baseline CRLF/Prettier היסטורי (R-33/R-17; ב-`main` **395**). **כל 30 הקבצים ששונו לעומת `main` עוברים lint נקי.** לא בוצע נרמול לקבצים לא קשורים (חריג: `HomeExercisePicker.tsx` — יוצא `CustomExerciseForm`, ו-`--fix` יישר 3 שורות prettier באותו קובץ).
- `git diff --check main` נקי (רווחים מיותרים בשני קובצי לוג של הראיות בקפסולה הוסרו; התוכן לא שונה). סריקת סודות ב-diff — נקייה; `nhnuuooyxamkkqqpcgmk` אינו מופיע.
- `src/routeTree.gen.ts`: ה-build המקומי מייצר אותו עץ בסדר שונה (R-30, diff ממוין = 0 שורות); הקובץ המחויב — שנוצר ע"י build בסדר של Lovable — לא נערך ידנית ולא הוחלף. מסלולים: 57 מודולים / 81 נתיבים; חדשים לעומת `main`: `/history`, `/running/project` בלבד.

## QA מובייל 390×844 RTL — מה נבדק (כולו עבר, 0 שגיאות console/pageerror, 0 גלילה אופקית)
1. מסך ראשי + שלושת הקיצורים · 2. `/running/project` · 3. שבוע נוכחי/קודם/הבא (שבועות נוצרים בכניסה, לא דורסים) · 4. עריכת מרוץ ותאריך מדויק (הסרגל → 2026-10-02) · 5. העדפות ונתוני בסיס (180/80) · 6. יצירת המלצה · 7. עריכת יום (עליות · 35 · הערה) · 8. אישור / דחייה / עדכון מפורש (revision יחיד לכל עדכון) · 9. גרסאות קודמות · 10. "דווח ביצוע" מיום מתוכנן (`?plan=`) · 11. 37 דק׳ 20 שנ׳, 6.25 ק״מ, 10,4 קמ״ש · 12. שמירה מיד אחרי ההקלדה האחרונה → `2240s / 6250m / 10.4 / קצב 358.4 derived` · 13. יציאה מיידית דרך הניווט וחזרה — הערך האחרון נשמר · 14. רענון — נשמר, ללא 404 · 15. הריצה מופיעה בשבוע ("בפועל השבוע: 1 ריצות · 6.25 ק״מ · 37:20") ובהיסטוריה המאוחדת · 16. כפיפות בטן 15, 20, 17 · 17. שכיבות סמיכה 19, 48, 32 · 18. יהלום · 19. רגילות / עלייה מלאה / הפוכות · 20. קפיצה בחבל (מדידת זמן) · 21. תרגיל משקולות מותאם: 10 חזרות, 2,5 ק״ג · 22. "תום" כהקשר בלבד (מוצג בהיסטוריה "יחד עם תום"; אין פרופיל) · 23. חזרה מכל מסך ששונה · 24–25. ללא overflow, ללא כפתורים מוסתרים, מטרות מגע ≥44px במסכים החדשים (טופס הריצה שומר על 36px של הריפו).
- מצב כאב → 7 ימי מנוחה, ללא המלצת ריצה. מרוץ ללא תאריך → אין taper מומצא (המלצה מציינת זאת). ריצה ישנה (מבנה לפני ההרחבה) נטענה ומופיעה בהיסטוריה. מפתחות `fitlog:*` — זהים לפני ואחרי; סמן בדיקה לא נמחק.
- גיבוי: ייצוא מהמסך כולל `races`, `trainingWeeks` (עם revisions), `coachSettings`, ריצה מקושרת ו-`training_partner`; שחזור מקובץ דרך המסך החזיר את כולם.

## מה לא אומת / מגבלות
- Phase 1 מאומת-משתמש וסנכרון ענן — ללא שינוי (T-05 פתוח). אין שינוי Auth/RLS/מיגרציות/Supabase.
- הקלדה **לפני** סיום ה-hydration (SSR) אינה נקלטת — התנהגות React/SSR כללית, לא באג של הטופס; ב-production ה-hydration מהיר.
- R-43: `treadmills.$id`, `locations.$id`, `exercises.$id` עדיין זורקים `notFound()` ב-loader בשרת (רענון → 404). מחוץ להיקף PR זה.
